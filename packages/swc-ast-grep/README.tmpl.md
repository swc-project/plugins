# @swc/plugin-swc-ast-grep

## Usage

.swcrc:

```json
{
  "jsc": {
    "experimental": {
      "plugins": [
        "@swc/plugin-swc-ast-grep",
        {
          "rules": "rules.yaml"
        }
      ]
    }
  }
}
```

${CHANGELOG}
